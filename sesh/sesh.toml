#  https://github.com/joshmedeski/sesh

blacklist = [
    "scratch",
]

sort_order = [
    "tmux",
    "tmuxinator", # shown first
    "config",
    "zoxide",
]

[default_session]
startup_command = "vim"
preview_command = "eza --oneline --all --icons=always --color=always {}"
startup_command_ignore_dirs = ["~/"]
